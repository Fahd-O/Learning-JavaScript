{
    let name = prompt("What is the name ?");

    if(name === "HostelCompass") (console.log("Assalamu 'alaykum warahmatu llahi wabarakaatuh"));//this also works if the action to be carried out after the condition is met is placed in curly braces
}